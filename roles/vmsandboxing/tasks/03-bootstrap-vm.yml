- name: Create and start virtual machine
  import_role:
    name: jm1.libvirt.server
  vars:
    domain: "{{ vm_name }}"
    hardware: "{{ _vm_options }}"
    image: "{{ vm_name }}"
    pool: "sandbox"
    image_uri: "{{ vm_image_url }}"
    volume_capacity: "100G"
  notify: start vm
